<Controls:MetroWindow x:Class="Playground.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:windows="clr-namespace:SoftFluent.Windows;assembly=SoftFluent.Windows"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Playground"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.Resources>
            <local:Model x:Key="model"/>
        </Grid.Resources>
        <DockPanel>
            <DockPanel  DockPanel.Dock="Top">
                <Label DockPanel.Dock="Left">Normal Binding:</Label>
                <Controls:NumericUpDown Value="{Binding SF}" HasDecimals="True"/>
            </DockPanel>
            <DockPanel DockPanel.Dock="Top">
                <windows:PropertyGrid  x:Name="sf" />
            </DockPanel>
            <DockPanel DockPanel.Dock="Top">
                <windows:PropertyGrid  x:Name="sf1" />
            </DockPanel>
            <DockPanel></DockPanel>
        </DockPanel>
    </Grid>
</Controls:MetroWindow>
