<Window x:Class="plainDataValidation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <TabControl>
            <TabItem Header="Then click me">
                <StackPanel>
                     <TextBox Height="20" x:Name="XTextBox" Text="{Binding X, Mode=TwoWay, ValidatesOnDataErrors=True}"  />
                    <TextBox Height="20" x:Name="YTextBox"  Text="{Binding Y, Mode=TwoWay, ValidatesOnDataErrors=True}"  />
                </StackPanel>
            </TabItem>
            <TabItem Header="click me first">
                <StackPanel>
                    <TextBox Height="20" Text="Any string"  />
                </StackPanel>
            </TabItem>
        </TabControl>
        
    </Grid>
</Window>
